# BEACON Scrapers — Dependencias Python
# =======================================
# Instalar con:
#   pip install beautifulsoup4 lxml
#
# Estas librerías son ADICIONALES al entorno del backend.
# El scraper comparte las mismas versiones de supabase, requests
# y python-dotenv que ya están instaladas en el entorno principal.
#
# ⚠️  NO correr "pip install -r requirements.txt" completo o baja
#     httpx y supabase a versiones viejas incompatibles con el backend.

# HTML parsing (para BCN, Cámara, Senado)
beautifulsoup4>=4.12
lxml>=5.0

# Playwright (para sitios con JS — Cámara, Senado)
# Solo instalar si vas a usar camara_scraper.py o senado_scraper.py
# playwright>=1.43
# → después de instalar: playwright install chromium
